# DAIC-WOZ 多模态抑郁风险筛查实验报告

**数据集：** DAIC-WOZ（189 名参与者，编号 300–492）  
**生成日期：** 2026-08-19

---

## 一、问题背景

抑郁症是全球高发且漏诊率极高的精神疾病。传统临床筛查依赖 PHQ-8 问卷，需要面诊时间，且依赖受访者主观报告，存在遗漏风险。能否从结构化访谈的记录（文字、声音、视频）中自动估计抑郁风险，是本项目要回答的核心问题。

DAIC-WOZ 数据集包含 189 名受试者与虚拟访谈者 Ellie 的完整对话记录，每人附有 PHQ-8 评分（0–24 分连续值，≥10 为抑郁阳性）。这是目前公开的最具代表性的临床访谈多模态数据集之一。

---

## 二、任务定义

- **主任务（回归）：** 根据访谈记录预测 PHQ-8 总分（0–24 连续值），以 Spearman ρ 和 RMSE 衡量。
- **辅任务（二分类）：** 以 PHQ-8 ≥ 10 为阈值判定抑郁阳性，以 Accuracy、F1、AP（PR 曲线面积）衡量。
- **数据划分：** train=107 人，dev=35 人，test=47 人（测试集在所有超参数确定后仅使用一次）。
- **额外协议：** LOSO（留一交叉验证，在 train+dev 142 人上进行）用于评估泛化稳定性，作为 dev 单次评估的校验。

---

## 三、方案设计

### 3.1 整体架构

采用"**LLM 提取语义特征 + 小型监督模型预测**"的分层架构，而非端到端黑箱。核心思路是：

1. 用大语言模型（LLM）阅读访谈转写，提取结构化的 PHQ-8 症状观察（每类症状是否出现、频率、严重程度），而非让 LLM 直接输出评分。
2. 将 LLM 提取的观察特征与传统信号处理特征（声学、视觉、时序）拼合，输入监督模型做校准预测。
3. 监督模型提供特征重要性，输出有可解释性支撑。

### 3.2 四条特征路

| 标识 | 来源 | 维度 | 提取方式 |
|------|------|------|----------|
| **tt**（转写时序） | 访谈逐字稿的时序统计 | 34 | `build_features.py`：停顿、语速、发言轮次、响应延迟等统计量 |
| **obs**（文本观察） | LLM 对转写的结构化阅读 | 93 | DeepSeek v4 Flash，`run_text_skill.py`，全 189 人 |
| **ac**（声学） | COVAREP 声学特征 | 83 | F0、共振峰、MFCC、发声质量等逐帧均值/分位数 |
| **vi**（视觉） | CLNF 面部 AU 激活 | 47 | AU 强度均值/激活率、注视方向、头部姿态变化量 |

### 3.3 LLM 观察 Skill 设计

[`skills/text_observation/skill.md`](skills/text_observation/skill.md) 定义了一套结构化 prompt，指导 LLM 针对 PHQ-8 的 9 个症状维度（情绪低落、失去兴趣、睡眠障碍、疲劳等）逐一判断：是否有证据、当前/历史性、频率档位、严重程度、明确/隐含、支撑/反对计数。每名参与者独立调用一次 API，结果缓存到 `outputs/skill_cache/` 避免重复费用。

### 3.4 预测模型

实验中依次引入三类预测模型：

- **Ridge：** `Ridge(alpha=1.0)`，线性基线，无需调参。
- **GBDT：** `GradientBoostingRegressor`，初始参数 `n_estimators=200, max_depth=3, lr=0.05`；经 train-only LOSO 超参搜索后调优。
- **XGBoost：** `XGBRegressor`，固定 `max_depth=3, lr=0.05, subsample=0.8`；对 `n_estimators`、`min_child_weight`、`colsample_bytree` 做网格搜索。
- **Stacking 集成：** Ridge + SVR（rbf 核）+ GBDT 三路基模型，经 train-only LOSO 搜索各自超参，在 train OOF 矩阵上训练 meta-Ridge（alpha 亦经搜索确定）。

### 3.5 评估与对比协议

- 所有超参数和架构决策在 train+dev 142 人上确定，测试集严格隔离。
- LOSO 协议与官方 dev 评估并行运行，两者一致才视为结果可信。
- `compare_configs.py` 对两个配置做配对 bootstrap（2000 次重采样），在差值本身上构造 95% CI，而非比较各自 CI 是否重叠。

---

## 四、实验过程

### 4.1 消融实验（官方 dev，n=35，默认 GBDT）

| 配置 | 特征 | RMSE | Spearman ρ | Acc | F1 | LOSO ρ |
|------|------|------|-----------|-----|-----|--------|
| B0 均值基线 | — | 6.573 | — | 0.657 | — | — |
| A1 | tt | 5.910 | 0.484 | 0.657 | 0.400 | 0.310 |
| A2 | obs | **4.566** | **0.746** | 0.743 | 0.571 | 0.663 |
| A3 | ac | 7.084 | -0.087 | 0.657 | 0.143 | 0.103 |
| A5 | vi | 6.875 | -0.031 | 0.600 | 0.000 | -0.071 |
| B1 | tt+obs | 4.603 | 0.777 | 0.657 | 0.250 | 0.693 |
| B2 | obs+ac | 4.514 | 0.742 | 0.771 | 0.600 | 0.633 |
| B3 | obs+ac+vi | 4.813 | 0.743 | 0.743 | 0.526 | 0.618 |
| B4 | tt+obs+ac+vi | 4.717 | 0.755 | 0.686 | 0.421 | 0.648 |

**obs 单路 Ridge：** RMSE=5.394，ρ=0.658，**Acc=0.800，F1=0.667**（dev 集分类最优单配置）。

### 4.2 GBDT 超参数搜索（train-only LOSO，n=107）

搜索空间：`n_estimators ∈ {100,200,300}`，`min_samples_leaf ∈ {1,3,5,10}`，`max_features ∈ {sqrt, 0.5, 1.0}`，共 36 组。

在 obs 单路上：默认参数 LOSO ρ=0.639 → 调优后 **0.690**（+0.051），RMSE 4.273 → **4.079**。  
在 tt+obs 上：默认 LOSO ρ=0.650 → 调优后 **0.682**（+0.032），RMSE 4.239 → **4.110**。  
最终采用折中参数 `n_estimators=100, min_samples_leaf=5, max_features="sqrt"`，该组合在两路 Top 5 中均出现。

### 4.3 调优后 GBDT（官方 dev，n=35）

| 配置 | ρ（调优前→后） | RMSE | F1 | AP | LOSO ρ |
|------|--------------|------|-----|-----|--------|
| obs | 0.746 → **0.807** | 4.177 | 0.527 | 0.765 | **0.710** |
| tt+obs | 0.777 → **0.818** | 4.282 | 0.444 | 0.740 | **0.720** |
| obs+ac | 0.742 → **0.784** | 4.381 | 0.556 | **0.797** | 0.699 |
| tt+obs+ac+vi | 0.755 → **0.796** | 4.713 | 0.470 | 0.771 | 0.677 |

所有配置 Spearman ρ 提升 0.04–0.06，且 LOSO 同步提升，排除 dev 集过拟合。

### 4.4 XGBoost 超参数搜索（train-only LOSO，n=107）

固定参数与调优 GBDT 对齐（`max_depth=3, lr=0.05, subsample=0.8`），对 `n_estimators ∈ {100,200,300}`、`min_child_weight ∈ {1,3,5,10}`、`colsample_bytree ∈ {0.5,0.7,1.0}` 做 36 组网格搜索。XGBoost 的 `min_child_weight` 与 sklearn GBDT 的 `min_samples_leaf` 作用相近——均用于控制叶节点最小样本数，防止过拟合。搜索结果用于与 GBDT 在 Stacking 集成中做对比和替换。

### 4.5 Stacking 集成（tt+obs，官方 dev，n=35）

架构：**Ridge + SVR（rbf 核）+ GBDT → meta-Ridge**。

训练协议：
1. 在 train（107 人）内做 train-only LOSO 搜索三路基模型超参（Ridge alpha、SVR C/epsilon、GBDT 参数沿用 4.2 最优值）。
2. 在 train OOF（107×3 矩阵）上搜索 meta-Ridge alpha，最优值为 0.001。
3. 基模型在整个 train 上全量训练，预测 dev；meta-Ridge 在 train OOF 上拟合，推断 dev 得最终分数。
4. LOSO（train+dev 142 人）评估：每折内层重建 OOF 并重新训练 meta，meta alpha 固定来自 train 阶段（轻微泄漏，已在结果中标注）。

最优基模型参数：`Ridge alpha=100.0`，`SVR C=1.0, epsilon=0.5`，`GBDT n_estimators=100, max_depth=3, lr=0.05`。

**Stacking dev 结果（n=35）：**

| 指标 | 值 |
|------|-----|
| Spearman ρ | **0.838**（95% CI [0.698, 0.923]） |
| RMSE | 4.278 |
| Accuracy | 0.714 |
| F1 | 0.333 |
| AP | 0.758 |
| Brier | 0.162 |

**Stacking LOSO train+dev（n=142，meta α 来自 train，轻微泄漏）：**

| 指标 | 值 |
|------|-----|
| Spearman ρ | **0.709**（95% CI [0.619, 0.782]） |
| RMSE | 4.214 |
| Accuracy | 0.775 |
| F1 | 0.512 |
| AP | 0.648 |
| Brier | 0.159 |

Stacking 在 dev 上 ρ=0.838 高于调优 GBDT（0.818），但 F1 从 0.444 降至 0.333，说明集成提升了排序相关性但牺牲了分类阈值性能；LOSO ρ=0.709 与调优 GBDT 的 0.720 接近，说明两者泛化能力相当。

---

## 五、最终测试集结果

> ⚠️ 测试集（n=47）已使用一次，结果不可再作为调参依据。

| 配置 | RMSE | Spearman ρ | 95% CI | Acc | F1 | Prec | Rec | AP |
|------|------|-----------|--------|-----|-----|------|-----|-----|
| B0 均值基线 | 6.411 | — | — | 0.702 | — | — | 0.000 | 0.298 |
| obs（Ridge） | 6.443 | 0.534 | [0.268, 0.720] | 0.723 | 0.519 | 0.538 | 0.500 | 0.507 |
| tt+obs（GBDT 调优） | 4.125 | 0.766 | [0.601, 0.877] | **0.830** | **0.714** | **0.714** | **0.714** | 0.772 |
| **tt+obs（Stacking）** | **4.065** | **0.816** | **[0.670, 0.900]** | 0.787 | 0.643 | 0.643 | 0.643 | **0.801** |

Stacking 在测试集上 Spearman ρ=0.816，高于调优 GBDT 的 0.766，RMSE 也略低（4.065 vs. 4.125），AP 达到 0.801。但分类 F1=0.643 低于 GBDT 的 0.714，Precision 与 Recall 同为 0.643，说明 Stacking 的回归排序能力更强，而 GBDT 在固定阈值二分类上更精准。

相较均值基线，Stacking RMSE 下降 36.6%，ρ=0.816 的 95% 置信区间下界为 0.670，置信区间不含零，结果统计显著。

---

## 六、特征重要性摘要（调优后 GBDT，obs 单路 Top 5）

| 特征 | 重要性 | 含义 |
|------|--------|------|
| obs_depressed_explicit | 0.089 | 参与者直接表达抑郁情绪次数 |
| obs_total_n | 0.086 | 总有效症状观察数 |
| obs_depressed_for_n | 0.076 | 抑郁情绪的历史性证据计数 |
| obs_depressed_current | 0.063 | 当前存在的抑郁证据计数 |
| obs_severity_sum | 0.060 | 症状严重程度加权和 |

在全配置（tt+obs+ac+vi）中，前 10 位特征有 9 个来自 obs；ac 最高贡献仅 `f0_p90`（0.017），vi 最高为 `gaze_wander_mean`（0.015）。

---

## 七、各配置定位总结

| 评估目标 | 最优配置 | 关键指标 |
|----------|----------|----------|
| 最高回归排序（测试集） | tt+obs Stacking | ρ=0.816，RMSE=4.065 |
| 最高分类 F1（测试集） | tt+obs GBDT（调优） | F1=0.714，Acc=0.830 |
| 最高 AP（测试集） | tt+obs Stacking | AP=0.801 |
| dev 集分类最优（未泛化） | obs Ridge | F1=0.667，Acc=0.800 |
| 最简可部署 | obs Ridge | 93 维线性模型，无需调参 |

---

## 附录：文件索引

| 文件 | 说明 |
|------|------|
| [`outputs/features/a2_text_observations.csv`](outputs/features/a2_text_observations.csv) | 文本观察特征，189 行 93 列 |
| [`outputs/features/a1_transcript_timing.csv`](outputs/features/a1_transcript_timing.csv) | 转写时序特征，189 行 34 列 |
| [`outputs/features/a3_acoustic.csv`](outputs/features/a3_acoustic.csv) | 声学特征，189 行 83 列 |
| [`outputs/features/a5_visual.csv`](outputs/features/a5_visual.csv) | 视觉特征，189 行 47 列 |
| [`run_experiment.py`](run_experiment.py) | 消融实验主脚本（参数已为调优值） |
| [`tune_gbdt.py`](tune_gbdt.py) | GBDT 超参数搜索脚本 |
| [`tune_xgboost.py`](tune_xgboost.py) | XGBoost 超参数搜索脚本 |
| [`run_stacking.py`](run_stacking.py) | Stacking 集成实验脚本 |
| [`calibrate_and_fuse.py`](calibrate_and_fuse.py) | 特征融合与校准 |
| [`app_ui.py`](app_ui.py) | Streamlit 实验面板 |
| [`llm_providers.py`](llm_providers.py) | LLM 多提供商接入层 |
| [`skills/text_observation/skill.md`](skills/text_observation/skill.md) | 文本观察 Skill Prompt |
