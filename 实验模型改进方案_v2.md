# DAIC-WOZ 实验模型改进方案 v2

> **文档性质：** 实验设计与执行方案，不是已经完成的实验结果。
>
> **项目目标：** 在不污染测试集的前提下，尽量把分类 Accuracy 推到 80% 左右，并把 PHQ-8 回归 RMSE 从当前约 4.1--5.2 降到 3.5 以内，继续冲击 3.0。
>
> **适用目录：** `C:\Users\LENOVO\daic_project`
>
> **最后审计基线：** 2026-08-13

---

## 1. 先看结论

当前项目最值得继续的方向不是把所有声学、视觉和文本特征继续堆进一个更复杂的模型，而是分三步解决三个不同问题：

1. **让 LLM 负责理解和排序。** LLM 能从长访谈中识别复杂语义，当前 D1 的 Spearman 排序能力最好。
2. **让一个只在训练数据上拟合的校准器负责 PHQ-8 数值尺度。** D1 的主要问题是绝对分数有系统性偏差，不是完全没有信号。
3. **让一个小型、正则化的残差模型只修正校准后的剩余误差。** 先使用结构化文本观察，只有在配对交叉验证中证明有稳定增益，才加入 timing、acoustic 或 visual。

推荐的 v2 架构为：

```text
Participant transcript
        |
        v
Text Observation Skill v2
(8 个 PHQ-8 维度、最近两周、频率、证据、时间范围、置信度)
        |
        +--------------------------+
        |                          |
        v                          v
Structured symptom features       D1 direct score
        |                          |
        +------------+-------------+
                     v
           Train-only calibration
           affine / isotonic
                     |
                     v
             Calibrated base score
                     |
                     v
            Regularized residual model
            Ridge first, ElasticNet second
                     |
                     v
             Final PHQ-8 score [0, 24]
                     |
                     +--> calibrated classifier probability
                     |
                     +--> explanation and quality flags
```

### 1.1 目标的现实解释

- `Accuracy >= 0.80` 可以作为阶段目标。当前 dev 集只有 35 人，错一个样本就会改变约 2.9 个百分点，因此必须用嵌套交叉验证和置信区间确认，而不能只看一次 dev。
- `RMSE <= 3.5` 是近期可验证目标；`RMSE around 3.0` 是挑战目标，不能在实验前承诺。
- 最终不能只报告 Accuracy。必须同时报告 Precision、Recall、Specificity、Balanced Accuracy、PR-AUC、Brier、RMSE、MAE 和 Spearman。

---

## 2. 研究问题：用 Context -> Problem -> Response 固定主线

本项目后续不要用“再试一个模型”作为研究问题。使用下面这条可以被证伪的主线。

### 2.1 Context：已经知道什么

DAIC-WOZ 的访谈同时包含语言、时间、声学和视觉信号。PHQ-8 是 8 个症状条目在最近两周频率上的总分，范围为 0--24。LLM 对长访谈的整体语义理解可能优于手工统计特征，但 LLM 直接生成的数值不一定遵守量表的绝对尺度。

### 2.2 Problem：现在的缺口是什么

当前结果显示：

- D1 的排序能力强，但 RMSE 不低，说明“谁更严重”和“应该给多少分”是两个不同问题。
- A2 结构化文本观察的 RMSE 较低，但排序能力不如 D1。
- 简单的全模态拼接没有证明声学和视觉能稳定提升，反而可能在 142 个有标签样本、200 多个候选特征的条件下引入噪声和过拟合。
- 当前评估工具还没有完整区分 Precision、PR-AUC、Recall 和 Specificity，标签中的 `PHQ8_Binary` 与 `PHQ8_Score >= 10` 也存在待核对样本。

### 2.3 Response：本方案要验证什么

> **研究问题：** 时间感知的 PHQ-8 症状结构化证据，加上训练集内分数校准和残差融合，能否在保留 LLM 排序能力的同时降低 PHQ-8 绝对误差，并提高分类性能？

### 2.4 可证伪假设

| 假设 | 预期现象 | 失败时的结论 |
|---|---|---|
| H1：D1 存在可校准的尺度偏差 | affine 或 isotonic 在嵌套 CV 中降低 RMSE | D1 的偏差不是稳定的全局尺度偏差，不能只靠后处理 |
| H2：结构化症状证据能解释 D1 的剩余误差 | `calibrated D1 + A2` 比 calibrated D1 的配对 RMSE 更低 | A2 与 D1 信息高度重复，或 encoder 噪声太大 |
| H3：质量门控比无约束多模态拼接更稳 | 只有高质量 acoustic/visual 样本被使用时，指标不恶化 | 当前声学/视觉特征不适合本任务或数据质量不足 |
| H4：分类校准能改善综合指标 | Accuracy、Precision、Recall、Balanced Accuracy 同时达到门槛 | 80% Accuracy 可能来自类别偏置，不能作为可靠筛查模型 |

本方案的贡献不是声称一定达到某个数字，而是建立一套能回答“为什么有效、何时失效、是否泛化”的实验协议。

---

## 3. 当前已核对的项目状态

### 3.1 特征表

当前应使用 `outputs/features/` 下的五张 189 人表：

| Lane | 文件 | 行数 | 列数 | 含义 |
|---|---|---:|---:|---|
| tt | `a1_transcript_timing.csv` | 189 | 35 | 转写时序、停顿、轮次、响应延迟 |
| obs | `a2_text_observations.csv` | 189 | 53 | LLM 提取的文本观察特征 |
| ac | `a3_acoustic.csv` | 189 | 84 | COVAREP 声学统计特征 |
| vi | `a5_visual.csv` | 189 | 48 | OpenFace/CLNF 视觉统计特征 |
| d1 | `d1_direct_scores.csv` | 189 | 4 | LLM 直接 PHQ-8 总分估计 |

**禁止使用：**

- `outputs/features/all_features.csv` 旧版 41 人表；
- `outputs/features/_backup_41p/` 中的任何表；
- 未记录来源、模型版本和生成时间的外部 CSV。

新的实验代码必须在合并前检查每个 lane 的 participant 覆盖率，并输出 inner join 后的样本数。不能因为某一条 lane 缺人而静默减少样本。

### 3.2 标签和划分

| Split | 样本数 | 用途 |
|---|---:|---|
| train | 107 | 拟合模型、校准器、阈值和 prompt 迭代 |
| dev | 35 | 官方 train -> dev 对照和方法选择 |
| test | 47 | 所有设计冻结后只评估一次 |

训练和开发合计 142 人。test 的 47 人不能用于 prompt、特征选择、校准参数、阈值或超参数调节。

### 3.3 已有 LOSO 基线

以下是当前项目主文档记录的 train+dev、142 人 LOSO 结果。它们是后续实验必须保留的基线，不是 v2 已经取得的结果。

| 配置 | Spearman | RMSE | 解释 |
|---|---:|---:|---|
| A1 tt | 0.310 | 5.903 | 仅时序 |
| A2 obs | 0.597 | 4.546 | 仅结构化文本观察 |
| A3 ac | 0.103 | 6.071 | 仅声学 |
| A5 vi | -0.071 | 6.617 | 仅视觉 |
| B4 tt+obs+ac+vi | 0.630 | 4.582 | 全模态 GBDT |
| D1 direct | 0.742 | 5.191 | LLM 直接打分 |

已有证据支持的工作判断：

1. D1 排序最强，但绝对尺度偏差明显。
2. A2 的绝对误差低于 D1，说明结构化观察具有可用的数值信号。
3. 声学、视觉当前没有稳定增益，不能因为“多模态听起来更完整”就默认保留。
4. 样本数远小于候选特征数，复杂 GBDT 不是默认最优解。

### 3.4 初步校准证据

已有只读验证得到一个初步 affine 校准：

```text
calibrated_score = clip(2.879 + 1.035 * d1_raw, 0, 24)
```

在 train 拟合、dev 评估的初步结果为：

| 方法 | RMSE | Spearman |
|---|---:|---:|
| D1 raw | 5.430 | 0.823 |
| affine | 4.094 | 0.823 |
| isotonic | 4.044 | 0.807 |

固定 `PHQ-8 >= 10` 阈值的 dev 结果为：

| 方法 | Accuracy | Precision | Recall | Balanced Accuracy |
|---|---:|---:|---:|---:|
| D1 raw | 71.4% | 75.0% | 25.0% | 60.3% |
| affine | 82.9% | 87.5% | 58.3% | 77.0% |
| isotonic | 82.9% | 71.4% | 83.3% | 83.0% |

这些结果只能说明“校准值得做”，不能当作最终泛化结果：dev 只有 35 人，而且方法是在同一批 train/dev 上探索出来的。142 人留一受试者验证中，初步 affine 校准约为 RMSE 4.136、MAE 3.265、Spearman 0.657，仍明显高于挑战目标 3.0。

---

## 4. 评估口径必须先锁定

### 4.1 二分类标签口径

当前 `train_split_Depression_AVEC2017.csv` 和 `dev_split_Depression_AVEC2017.csv` 同时包含 `PHQ8_Binary` 和 `PHQ8_Score`。已发现至少一个需要核对的样本：participant 409 的 `PHQ8_Score=10`，但 `PHQ8_Binary=0`。

在官方比赛定义确认前，不要混用两种标签。执行 P1 时必须生成一张标签审计表：

| participant_id | PHQ8_Score | official PHQ8_Binary | derived_score_ge_10 | match |
|---:|---:|---:|---:|---|

推荐报告方式：

- **主结果：** 使用比赛官方规定的标签字段，明确写出字段名和版本。
- **敏感性分析：** 额外报告 `PHQ8_Score >= 10` 的派生标签结果。
- 如果两种口径不一致，不能把它们的 Accuracy 合并成一个数字。

### 4.2 指标定义

| 指标 | 定义 | 越大/越小越好 | 用途 |
|---|---|---|---|
| Accuracy | `(TP+TN)/(TP+TN+FP+FN)` | 越大 | 直观总体分类正确率 |
| Precision | `TP/(TP+FP)` | 越大 | 预测阳性中有多少真实阳性 |
| Recall / Sensitivity | `TP/(TP+FN)` | 越大 | 不漏掉阳性 |
| Specificity | `TN/(TN+FP)` | 越大 | 不误报阴性 |
| Balanced Accuracy | `(Recall+Specificity)/2` | 越大 | 类别不平衡时更可靠 |
| PR-AUC / Average Precision | 由连续分数产生的 precision-recall 曲线面积 | 越大 | 连续风险排序 |
| Brier Score | `mean((p-y)^2)` | 越小 | 概率校准 |
| RMSE | `sqrt(mean((score-y)^2))` | 越小 | PHQ-8 绝对误差 |
| MAE | `mean(abs(score-y))` | 越小 | 对离群误差更稳健 |
| Spearman | 真实分数与预测分数的秩相关 | 越大 | 排序能力 |

当前 [eval_utils.py](C:/Users/LENOVO/daic_project/eval_utils.py) 中名为 `pr_auc` 的实现实际调用 `average_precision_score`。它不是 Precision，P1 必须把字段名和打印输出改清楚。

### 4.3 统计协议

每个候选方案必须同时报告：

1. **Official split：** train 拟合，dev 评估，用于和现有文献及旧实验对照。
2. **Nested repeated CV：** 在 train+dev 内部做外层评估，所有校准、特征选择、阈值和超参数只在外层训练折内拟合。
3. **LOSO：** 保留现有 142 人 leave-one-subject-out 结果，用于与历史 baseline 对比。
4. **Paired bootstrap：** 对同一批 participant 的逐人预测计算模型差值置信区间。

最终 test 评估必须发生在所有配置、prompt、特征、校准器和阈值冻结之后，而且只发生一次。

### 4.4 建议的阶段门槛

| 阶段 | 近期放行线 | 挑战目标 |
|---|---:|---:|
| 回归 | RMSE <= 3.5 | RMSE around 3.0 |
| 分类 | Accuracy >= 0.80 | Accuracy >= 0.80 且不牺牲 Recall |
| 阳性识别 | Precision >= 0.70，Recall >= 0.60 | Precision 和 Recall 都稳定提升 |
| 综合分类 | Balanced Accuracy >= 0.75 | >= 0.80 |
| 统计可靠性 | 配对差值方向稳定，不能只靠单个 dev | 95% CI 大部分不跨 0 |

“Accuracy 达到 80% 但 Recall 只有 20%”不算成功；“RMSE 降低但 Spearman 和 Balanced Accuracy 大幅下降”也不应直接接受。

---

## 5. v2 总体架构和数据流

### 5.1 训练阶段

```text
train split
  |
  +--> prompt / Skill 版本选择（只看 train）
  +--> 结构化特征编码
  +--> D1 affine / isotonic 校准器
  +--> residual Ridge / ElasticNet
  +--> classifier + probability calibration + threshold
  |
  +--> dev 只用于官方比较和方法选择
```

### 5.2 推理阶段

```text
new transcript
  |
  +--> Skill v2 JSON
  +--> deterministic encoder
  +--> D1 raw score
  +--> frozen calibrator
  +--> frozen residual model
  +--> final PHQ-8 score and probability
```

推理阶段不能重新根据当前样本的真实标签或 test 结果修改 prompt、校准参数或阈值。

### 5.3 为什么不直接做大模型端到端回归

- PHQ-8 总分是低范围离散量表，直接自由生成数值容易有尺度漂移。
- 142 个训练样本不适合支撑大量可训练参数。
- 分层结构允许分别检查“证据提取错误”“尺度校准错误”和“残差模型错误”。
- 可解释性更好：每个最终分数都能追溯到结构化证据、校准参数和残差修正。

---

## 6. 分阶段实施路线

### P0：版本冻结、数据清点和复现审计

**目标：** 先确保下一次结果可以解释和复现。

#### 必做事项

1. 锁定当前五张 189 人特征表的文件哈希、行数、列数和 participant 集合。
2. 明确 `train/dev/test` 成员，不读 test 标签进行任何调参。
3. 为每条 LLM 特征记录 provenance：participant、Skill 名称、Skill 版本、prompt hash、请求模型、实际返回模型、provider、temperature/reasoning、生成时间和 encoder 版本。
4. 将当前已有的 189 人 CSV 与旧 41 人 CSV 分开命名，禁止自动 fallback 到 `all_features.csv`。
5. 检查 `a2_text_observations.csv` 中的字段是否能由当前 [run_text_skill.py](C:/Users/LENOVO/daic_project/run_text_skill.py) 的 `encode_features()` 重新生成。若不能，说明特征表和代码版本不一致，必须先记录差异。

#### 建议文件

```text
feature_manifest.json
outputs/manifests/feature_manifest_YYYYMMDD.json
outputs/manifests/label_audit_YYYYMMDD.csv
```

#### manifest 最小字段

```json
{
  "manifest_version": "1.0.0",
  "created_at_utc": "2026-08-13T00:00:00Z",
  "project_root": "C:/Users/LENOVO/daic_project",
  "label_policy": {
    "primary_field": "PHQ8_Binary",
    "regression_field": "PHQ8_Score",
    "threshold_for_sensitivity_analysis": 10,
    "verified": false
  },
  "lanes": [
    {
      "name": "obs",
      "path": "outputs/features/a2_text_observations.csv",
      "rows": 189,
      "columns": 53,
      "sha256": "TO_BE_FILLED",
      "encoder_version": "legacy-unknown"
    }
  ],
  "llm_provenance_required": [
    "participant_id",
    "skill_version",
    "prompt_sha256",
    "model_requested",
    "model_actual",
    "provider",
    "generation_timestamp_utc"
  ]
}
```

**放行条件：** 任意实验结果都能回答“用了哪一版数据、哪一版 prompt、哪一版 encoder”。

### P1：统一评估工具和标签口径

**目标：** 修复“指标名字和实际计算不一致”以及标签口径不明确的问题。

#### 修改 [eval_utils.py](C:/Users/LENOVO/daic_project/eval_utils.py)

- 增加 Accuracy、Precision、Recall、Specificity、Balanced Accuracy、Brier Score。
- 将 `average_precision_score` 明确命名为 `average_precision` 或 `pr_auc_ap`。
- 保留 RMSE、MAE、Spearman、Pearson，但在样本过少或预测常数时返回明确的 missing 状态。
- 所有分类指标接收同一个明确的 `y_binary`，不要在不同函数里隐式重建标签。
- 输出 `label_policy`、`split`、`n`、正类比例和阈值来源。
- 将 bootstrap seed、重复次数和 paired subject IDs 写入结果 JSON。

#### 建议增加的测试

```text
tests/test_eval_utils.py
tests/test_label_policy.py
```

至少覆盖：全阴性、全阳性、预测常数、PHQ-8=10 边界、缺失值、标签字段不一致。

**放行条件：** 同一个逐人预测文件在命令行、JSON 和报告中得到相同指标；指标名称不会把 AP 写成 Precision。

### P2：D1 分数校准

**目标：** 先不改变 LLM 输出，只修正其与 PHQ-8 量表的系统性尺度差异。

#### 2.1 主方法：affine calibration

```python
base = np.clip(a + b * d1_raw, 0.0, 24.0)
```

其中 `a` 和 `b` 只能在当前训练折拟合。`dev` 和外层验证折不能参与拟合。

#### 2.2 对照方法

1. `LinearRegression` 或带轻微正则的 `Ridge` affine calibration。
2. `IsotonicRegression(out_of_bounds="clip")`。
3. 单调 spline 作为后续扩展，不作为第一版默认方法。

affine 应作为主结果，因为只学习两个参数，在 142 个样本上更稳定、也更容易解释。isotonic 作为保序对照，不能仅因为 dev 上数字更好就自动选用。

#### 2.3 回归伪代码

```python
for outer_train, outer_valid in outer_cv.split(ids):
    calibrator = fit_calibrator(
        d1_raw[outer_train],
        y_score[outer_train],
        method="affine",
    )
    base_valid = calibrator.predict(d1_raw[outer_valid])
    base_valid = np.clip(base_valid, 0, 24)
    report(y_score[outer_valid], base_valid)
```

禁止以下做法：

- 用全体 142 人先拟合一次校准器，再把这组预测当作无偏 LOSO 结果；
- 根据 dev 的单次 RMSE 反复选择复杂校准方法；
- 用 test 的真实分数估计 `a`、`b` 或阈值。

**放行条件：** calibrated D1 在重复嵌套 CV 中相对 D1 raw 的 RMSE 差值方向稳定，且 Spearman 不被无意义地破坏。

### P3：Text Observation Skill v2

**目标：** 把当前 v1 的“可验证观察”扩展为“时间感知的弱监督症状证据”，但仍不让 Skill 直接诊断或输出最终分数。

当前 v1 的重要约束必须保留：

- 只使用 Participant 的原文作为证据；
- 每个 quote 必须是原文可验证子串；
- Ellie 的话只能作为上下文，不能作为症状证据；
- 支持 `evidence_for`、否定/反证和 positive signal；
- 证据不足时允许 abstain；
- 不输出诊断，不直接输出 PHQ-8 总分。

#### 3.1 v2 每个条目的建议 schema

```json
{
  "dimension": "sleep",
  "current_2_weeks": "present | absent | unknown",
  "frequency": "not_at_all | several_days | more_than_half | nearly_every_day | unknown",
  "severity": 0,
  "evidence_for": [
    {
      "quote": "verbatim participant substring",
      "turn_index": 123,
      "temporal_scope": "current | historical | unclear",
      "strength": "explicit | implicit"
    }
  ],
  "evidence_against": [],
  "confidence": "low | medium | high",
  "ambiguity_reason": null
}
```

顶层 JSON 建议包含：

```json
{
  "schema_version": "text_observation_v2.0.0",
  "items": [],
  "positive_signals": [],
  "safety_flags": {
    "self_harm_explicit": false,
    "self_harm_ideation_possible": false,
    "evidence_quote": null
  },
  "data_sufficiency": "ok | thin | insufficient",
  "abstained": false,
  "anomalies": []
}
```

#### 3.2 关键规则

1. PHQ-8 关注最近两周；历史症状必须标记为 `historical`，不能与当前症状等权。
2. `frequency` 和 `severity` 分开，不能用一个模糊的“严重程度”字段代替。
3. `evidence_for` 和 `evidence_against` 分开，明确记录支持和反驳信息。
4. 不使用自由文本 Chain-of-Thought 作为模型特征。可以要求模型做内部分段分析，但只保存结构化结果和可验证 quote。
5. 如果没有 PHQ-8 每条目的人工真值，必须把这些字段称为“弱监督结构化症状特征”，不能声称恢复了真实 item score。
6. Skill 版本升级后，新缓存不能与 v1 缓存静默混合。

#### 3.3 质量验证

- quote verification：检查 quote 是否为对应 Participant turn 的原文子串。
- schema validation：使用 JSON Schema 拒绝非法字段和非法枚举值。
- temporal audit：随机抽样检查模型是否把童年/过去症状当成最近两周。
- abstention audit：检查短文本和证据不足样本是否被强行填分。
- inter-version audit：v1 与 v2 在同一批 train 样本上的变化必须可追踪。

**放行条件：** JSON 合法率、quote 验证率、时间范围抽检结果达到预设标准，并且结构化特征在嵌套 CV 中至少不劣于旧 A2。

### P4：症状级特征编码

**目标：** 把 v2 JSON 转为低维、稳定、可解释的数值特征。

#### 4.1 每个 PHQ-8 条目的建议特征

对 8 个维度分别生成：

- `current_present`
- `frequency_ordinal`
- `severity`
- `evidence_for_count`
- `evidence_against_count`
- `explicit_count`
- `implicit_count`
- `historical_count`
- `temporal_ambiguity`
- `confidence_low / medium / high`
- `quote_coverage`

全局特征可以包括：

- `structured_sum_weak`
- `present_item_count`
- `unknown_item_count`
- `positive_signal_count`
- `data_sufficiency`
- `abstained`
- `safety_flag`

不要一次性把所有原始文本、长 rationale、quote 拼接进 GBDT。quote 保留用于审计和解释，模型输入先使用结构化计数和质量特征。

#### 4.2 缺失值和质量门控

- 结构化字段缺失要显式增加 missing indicator。
- `data_sufficiency=insufficient` 时可以让残差模型降低修正幅度，而不是填入一个看似精确的分数。
- acoustic/visual 缺失或质量低时，不应把全局均值伪装成真实测量。

**放行条件：** encoder 的输出列、顺序、版本和缺失规则固定，并能从同一 JSON 重建完全相同的向量。

### P5：校准分数上的残差融合

**目标：** 让结构化观察只学习 D1 校准后没有解释的部分，避免模型重新学习一个不稳定的 PHQ-8 总分。

#### 5.1 推荐公式

```text
base = calibrated_d1
residual_target = y_true - base
residual_hat = residual_model(structured_features)
final_score = clip(base + alpha * residual_hat, 0, 24)
```

先从 Ridge 开始，再比较 ElasticNet。`alpha`、正则强度、标准化、缺失值填充和特征筛选都必须在训练折内完成。

#### 5.2 特征加入顺序

```text
E0  D1 raw
E1  calibrated D1
E2  A2 structured observations
E3  calibrated D1 + A2 residual correction
E4  E3 + transcript timing
E5  E3 + quality-gated acoustic
E6  E3 + quality-gated visual
```

推荐始终保留 `base`、`residual_hat`、`data_quality` 作为独立输出列，便于解释每个样本的最终分数来自哪里。

#### 5.3 不能做的事情

- 不能直接把 200 多个原始特征送入无约束 GBDT，然后只报告一次 dev 结果。
- 不能在全体 142 人上先做相关性筛选，再跑 LOSO。
- 不能因为声学或视觉在单个样本上“看起来合理”就保留整个 lane。
- 不能让 D1 的 `binary_estimate` 直接决定正式分类结果。

**放行条件：** `E3` 在 paired nested CV 中相对 `E1` 的 RMSE 有稳定下降，同时 Accuracy、Balanced Accuracy、Recall 不出现不可接受的退化。

### P6：独立分类校准

**目标：** 回归分数和分类概率使用不同的校准逻辑，但共享冻结的基础证据。

#### 6.1 输入

优先输入：

- calibrated D1 score；
- structured symptom sum；
- residual correction；
- data quality flags；
- 可选的少量 timing 特征。

不要直接使用 D1 的 `binary_estimate` 作为最终类别。

#### 6.2 模型和阈值

- 主模型：Logistic Regression。
- 概率校准：Platt scaling 为主，Isotonic 为对照。
- 分类阈值：在训练折内选择，不能固定复制 dev 上最优阈值到所有外层折。
- 阈值选择目标：先约束 Recall 和 Precision，再在约束范围内优化 Balanced Accuracy 或 Accuracy。

一个可执行的选择规则示例：

```text
在训练折候选阈值中：
1. Recall >= 0.60；
2. Precision >= 0.70；
3. 在满足 1、2 的阈值中选择 Balanced Accuracy 最高者；
4. 若没有阈值同时满足，报告“未达到阶段门槛”，不偷偷放宽条件。
```

#### 6.3 低置信度样本

如果最终用于辅助筛查而不是强制分类，可以增加 abstention/risk-coverage 分析：

- 置信度低或多个 Skill 重复采样不一致时，标记为人工复核；
- 同时报告覆盖率和剩余样本上的性能；
- 不把拒答样本从 Accuracy 分母中删除而不报告 coverage。

**放行条件：** 分类概率的 Brier 和校准曲线不恶化，且 Accuracy 80% 不是通过极端偏向多数类获得。

### P7：最终冻结和测试集评估

只有满足以下条件才进入最终 test：

1. 评估函数和标签口径已冻结。
2. Skill prompt、schema、encoder 和特征 manifest 已冻结。
3. 校准方法、残差模型、分类器、阈值和质量门控已冻结。
4. 已完成官方 train -> dev、LOSO 和 nested CV 报告。
5. 已保存所有候选配置和失败配置，不再根据 test 结果回改。

最终流程：

```text
train + dev 重新拟合冻结方案
       |
       v
对 test 47 人生成一次预测
       |
       v
保存逐人预测、版本、哈希、时间和完整指标
       |
       v
锁定 test 结果，不再调参
```

---

## 7. 实验矩阵和比较规则

### 7.1 候选配置

| 编号 | 配置 | 模型 | 目的 |
|---|---|---|---|
| E0 | D1 raw | 无训练 | 保留当前 black-box 对照 |
| E1 | D1 affine | train-only affine | 验证尺度校准 |
| E1b | D1 isotonic | train-only isotonic | 保序校准对照 |
| E2 | A2 structured | Ridge / GBDT | 观察结构化文本单独能力 |
| E3 | E1 + A2 residual | Ridge | 验证残差修正 |
| E4 | E3 + tt | Ridge | 验证时序是否补充剩余信息 |
| E5 | E3 + ac quality-gated | Ridge | 声学增量实验 |
| E6 | E3 + vi quality-gated | Ridge | 视觉增量实验 |
| E7 | E3 + ac + vi | Ridge | 只在 E5/E6 单独有效后尝试 |

### 7.2 选择原则

主配置不是“单个指标最高”的配置，而是满足以下排序的最小模型：

1. 先看 nested CV 的 RMSE 和 MAE。
2. 再看 Spearman 是否保留主要排序能力。
3. 再看 Accuracy、Precision、Recall、Specificity、Balanced Accuracy 和 Brier 的联合表现。
4. 新 lane 必须在 paired comparison 中显示稳定净增益，否则剔除。
5. 在性能接近时优先选择特征少、参数少、解释清晰的配置。

建议的保留门槛：新增模块相对父配置的 paired RMSE 至少下降约 0.15，且差值方向在重复外层折中大体一致；这个数值是工程筛选线，不是显著性检验的替代物。

### 7.3 结果表模板

每个配置保存如下字段：

```text
config_id
data_manifest_hash
label_policy
split_protocol
outer_fold_count
inner_fold_count
model_type
calibration_type
feature_names_hash
n_subjects
rmse
rmse_ci95
mae
mae_ci95
spearman
spearman_ci95
accuracy
precision
recall
specificity
balanced_accuracy
average_precision
brier
threshold
per_subject_predictions_path
```

报告中必须同时提供总体指标和逐人预测文件，不能只保存一行平均分数。

---

## 8. 代码文件分工

### 8.1 第一批必须改

#### `eval_utils.py`

- 完善分类和回归指标；
- 明确 AP 与 Precision 的命名；
- 统一标签策略；
- 输出可复现的 bootstrap 信息。

#### `run_text_skill.py`

- 支持 Skill schema 版本；
- 增加结构化字段 encoder；
- 保存 quote verification 和 provenance；
- 不覆盖不同版本的缓存。

#### `skills/text_observation/skill.md`

- 从 v1 升级到 v2；
- 增加最近两周、频率、严重度、时间范围、正反证据和置信度；
- 继续禁止诊断、最终分数和不可验证的自由文本证据。

#### `calibrate_and_fuse.py`（新建）

负责：

- affine / isotonic 校准；
- residual Ridge / ElasticNet；
- nested CV；
- 分类概率校准和阈值选择；
- paired bootstrap；
- 保存 per-subject prediction 和每折参数。

### 8.2 第二批再改

#### `run_experiment.py`

只有在新协议独立验证后，才把 calibrated D1 和 residual fusion 接入统一入口，避免破坏已有 baseline 的可复现性。

#### `PROJECT_DOCUMENTATION.md`

新方案验证后再补充结果。不要现在把未实现的 v2 结构写成当前系统已经完成。

### 8.3 建议新增目录

```text
experiments/
  configs/
  protocols/
  reports/
tests/
outputs/
  manifests/
  calibrated/
  predictions/
  reports/
```

---

## 9. 执行顺序和命令

下面分为“现有代码可执行命令”和“v2 实现后命令”。不要在新脚本还没实现前直接复制未来命令运行。

### 9.1 进入项目和检查环境

PowerShell：

```powershell
Set-Location 'C:\Users\LENOVO\daic_project'
& '.\.venv\Scripts\python.exe' --version
& '.\.venv\Scripts\python.exe' -c "import pandas, numpy, sklearn; print('dependencies ok')"
```

如果还没有虚拟环境：

```powershell
& 'python' 'setup_and_run.py' --no-ui
```

新电脑也可以双击 `setup_and_run.bat`，界面默认在 `http://localhost:8501`。

### 9.2 当前数据处理流程

```powershell
Set-Location 'C:\Users\LENOVO\daic_project'

# 第一次才需要，从 *_P.zip 抽取必要文件
& '.\.venv\Scripts\python.exe' 'extract_from_zips.py'

# 生成 tt / acoustic / visual 三条确定性特征 lane
& '.\.venv\Scripts\python.exe' 'build_features.py'

# 只在需要重新调用 LLM 时运行；先用 train 小样本验证
& '.\.venv\Scripts\python.exe' 'run_text_skill.py' '--skill' 'text_observation' '--only' '300'
& '.\.venv\Scripts\python.exe' 'run_text_skill.py' '--skill' 'd1_direct_scoring' '--only' '300'
```

`extract_from_zips.py --delete-zip` 会删除原始 zip，除非已确认备份和抽取结果，否则不要把它作为默认命令。

### 9.3 复现当前 baseline

```powershell
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'tt' '--model' 'both'
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'obs' '--model' 'both'
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'ac' '--model' 'both'
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'vi' '--model' 'both'
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'tt,obs,ac,vi' '--model' 'both'
& '.\.venv\Scripts\python.exe' 'run_experiment.py' '--lanes' 'd1'
```

每次运行后记录输出 JSON 文件名，不要只复制命令行最后一行结果。

### 9.4 v2 实现后的建议命令

以下是目标接口，只有在 `calibrate_and_fuse.py` 完成并通过测试后才使用：

```powershell
& '.\.venv\Scripts\python.exe' 'calibrate_and_fuse.py' `
  '--lanes' 'd1,obs' `
  '--calibration' 'affine,isotonic' `
  '--residual-model' 'ridge,elasticnet' `
  '--protocol' 'official,nested_loso' `
  '--seed' '42'
```

正式实验建议先跑：

```text
E0 -> E1 -> E1b -> E2 -> E3
```

只有 E3 证明有效，才继续：

```text
E3 -> E4 -> E5 -> E6 -> E7
```

### 9.5 结果比较

现有 `compare_configs.py` 支持同一 participant 集合上的 paired bootstrap。v2 结果也必须沿用 paired comparison 的思想：

```powershell
& '.\.venv\Scripts\python.exe' 'compare_configs.py' `
  'outputs/results/exp_d1_*.json' `
  'outputs/results/exp_obs_*.json' `
  '--label' 'LOSO'
```

实际运行时请将通配符替换为具体文件名，避免误比较旧版本结果。

---

## 10. 质量检查和故障定位

| 现象 | 优先检查 | 不要直接做的事 |
|---|---|---|
| 189 人表变成 41 人 | 是否读到了旧 `all_features.csv` | 不要继续训练并把结果当新版 |
| 多 lane 样本数突然下降 | participant inner join、缺失 lane、ID 类型 | 不要静默丢人 |
| dev 很好、nested CV 很差 | 校准器或特征选择是否看到了验证折 | 不要用 dev 结果证明泛化 |
| RMSE 降了、Recall 接近 0 | 分类阈值和类别比例 | 不要只报 Accuracy |
| 全模态比 obs 更差 | acoustic/visual 质量、特征数和正则化 | 不要强行保留全部模态 |
| isotonic 波动很大 | 样本量和折内单调拟合 | 不要仅凭一次 dev 选择 isotonic |
| Skill quote 验证失败 | 模型输出是否改写原文、turn_index 是否错位 | 不要把 paraphrase 当证据 |
| 重新运行后特征改变 | prompt、模型实际版本、temperature、缓存版本 | 不要覆盖旧缓存 |
| 标签边界结果矛盾 | `PHQ8_Binary` 与 `PHQ8_Score` 的官方定义 | 不要混合两个标签口径 |

### 10.1 何时停止继续加模型

出现以下任一情况时，应停止增加模型复杂度，先回到数据或评估审计：

- 新模块只在 dev 上提升，nested CV 不提升；
- 加入一个 lane 后 paired RMSE 和 Balanced Accuracy 同时恶化；
- 结果对随机种子、折划分或少数样本极端敏感；
- provenance 缺失，无法知道特征来自哪个 prompt/model 版本；
- test 已经被看过却还想继续调参。

---

## 11. 数据安全和研究纪律

### 11.1 DAIC-WOZ 数据

- 原始转写、音频、视频、COVAREP、CLNF 等受协议限制的数据不能上传到公开云端或未获许可的第三方服务。
- 只有在确认服务符合项目协议时，才允许把原始 transcript 发送给外部 LLM provider。
- 可共享的统计特征表也必须保留来源和版本信息，不能把可逆的原文 quote 与普通特征包混在一起公开。

### 11.2 API key

- `data/skill_models.json` 中不能长期保存明文 key。
- 已经暴露或怀疑暴露的 key 应立即撤销、轮换。
- 后续应改为环境变量或本机未提交配置，manifest 只记录 provider、模型和 key 是否存在，不记录 key 内容。

### 11.3 Test discipline

- test 47 人只在最终配置冻结后评估一次。
- 不能根据 test 错例修改 prompt、特征、阈值或模型。
- 如果已经看过 test 结果后继续修改，必须把该 test 结果标记为探索性、不能作为最终报告。

### 11.4 Skill 版本纪律

- 不要让多人同时改同一个 `skills/*/skill.md`。
- 每次 prompt 变化都增加 Skill 版本号和 prompt hash。
- 只允许用 train 样本做 prompt 迭代；dev 用于有限方法选择；test 不参与迭代。
- 不保存或使用不可验证的自由文本 Chain-of-Thought 作为训练特征。

---

## 12. 最终报告应该长什么样

最终实验报告至少包含以下内容：

### 12.1 数据和协议

- 数据版本和 manifest hash；
- train/dev/test 样本数；
- 标签字段和阈值；
- 特征 lane 覆盖率；
- Skill、模型、prompt、encoder 版本；
- official split、nested CV、LOSO 的具体定义。

### 12.2 主要结果

```text
Configuration: E3 calibrated D1 + A2 residual Ridge
Regression: RMSE, MAE, Spearman with 95% CI
Classification: Accuracy, Precision, Recall, Specificity,
                Balanced Accuracy, Average Precision, Brier
Protocol: repeated nested CV / official train->dev / LOSO
N: 142 labeled participants
```

### 12.3 失败和负结果

报告 acoustic、visual、复杂 GBDT 等没有稳定增益的结果。负结果不是浪费，它能说明模型为什么最终只保留少量高质量特征。

### 12.4 解释性检查

至少抽取三类样本做审计：

- 低分且校准正确；
- 高排序分但绝对分被校准修正；
- D1 和 residual model 分歧最大。

每个样本展示：结构化症状证据、quote、quality flags、D1 raw、calibrated base、residual correction 和 final score。不要把模型合理性解释成临床诊断。

---

## 13. 研究和文献对话

下面的文献只用于说明设计方向，不替代本项目自己的交叉验证结果：

1. Chen et al., **Depression Detection in Clinical Interviews with LLM-Empowered Structural Element Graph**, NAACL 2024。核心启发是先让 LLM 抽取结构元素，再交给结构化监督模型，而不是让 LLM 独立承担最终预测。
   - https://aclanthology.org/2024.naacl-long.452/
2. Sadeghi et al., **Harnessing multimodal approaches for depression detection using large language models and facial expressions**, npj Mental Health Research 2024。启发是文本指标和机器学习预测可以分层，多模态必须经过质量控制。
   - https://doi.org/10.1038/s44184-024-00112-8
3. Nguyen et al., ACL 2022。启发是把 PHQ 症状结构约束引入预测，以提高可解释性和泛化性。
4. **Calibrating Large Language Models**, NAACL 2024 survey。可参考 temperature scaling、feature-based calibrator、ensemble consistency，但本项目优先采用小样本更稳的 affine/isotonic。
   - https://aclanthology.org/2024.naacl-long.366/
5. **The Art of Abstention**, ACL 2021。启发是低置信度样本可以转人工，并用 risk-coverage 评估，而不是把所有样本都强行自动分类。
   - https://aclanthology.org/2021.acl-long.84/

本项目真正要加入的对话是：在小样本 DAIC-WOZ 条件下，LLM 的语义排序、量表校准和结构化残差修正如何分工，才能得到既准确又可审计的 PHQ-8 预测。

---

## 14. 立即执行清单

按下面顺序推进，不要跳到最后一步：

### 第 1 步：冻结和审计

- [ ] 生成五张 189 人特征表的 manifest 和 SHA-256。
- [ ] 输出 train/dev/test participant 集合和交集检查。
- [ ] 审计 participant 409 及所有 `PHQ8_Binary != (PHQ8_Score >= 10)` 的样本。
- [ ] 检查 189 人 CSV 与当前 `run_text_skill.py` encoder 的版本差异。
- [ ] 撤销并轮换可能暴露的 API key。

### 第 2 步：修正评估

- [ ] 修改 `eval_utils.py`，加入完整分类指标。
- [ ] 将 AP 与 Precision 分开命名。
- [ ] 为边界、缺失和常数预测增加单元测试。
- [ ] 复现 A2、B4、D1 的旧 baseline。

### 第 3 步：验证校准

- [ ] 新建 `calibrate_and_fuse.py` 的 affine 和 isotonic 对照。
- [ ] 先做 official train -> dev，再做 nested CV/LOSO。
- [ ] 保存每折校准参数和逐人预测。
- [ ] 只有校准在 nested CV 中有效，才进入残差融合。

### 第 4 步：升级 Skill 和 encoder

- [ ] 建立 `text_observation_v2.0.0` schema。
- [ ] 保留 participant-only quote 验证、否定证据和 abstention。
- [ ] 用 train 迭代 prompt，不能看 test 调整。
- [ ] 重新生成或明确标记 v2 特征缓存，不与 v1 静默混合。

### 第 5 步：残差和分类

- [ ] 先跑 calibrated D1 + A2 Ridge residual。
- [ ] 再按顺序尝试 timing、quality-gated acoustic、quality-gated visual。
- [ ] 用 Logistic + Platt/Isotonic 做独立分类校准。
- [ ] 同时检查 Accuracy、Precision、Recall、Balanced Accuracy 和 Brier。

### 第 6 步：最终冻结

- [ ] 保存候选配置和失败配置。
- [ ] 冻结代码、Skill、特征、标签和阈值。
- [ ] 用 train+dev 重训一次。
- [ ] test 47 人只评估一次并归档。

---

## 15. 一句话判断标准

如果一个改动只能让 dev Accuracy 变高，却不能在嵌套交叉验证中同时改善 RMSE、Balanced Accuracy 或校准质量，就不要把它当作模型优化成功。

真正值得保留的方案应该满足：

```text
LLM 负责提取可验证语义和排序信号
小模型负责学习 PHQ-8 的绝对尺度和残差
评估协议负责防止泄漏
manifest 负责保证结果可复现
```

