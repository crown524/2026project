# 如何在新电脑上运行本项目

面向拿到 U 盘包的人。全程约 10 分钟，其中 8 分钟是等依赖下载。

## 前提

- Windows 10/11（Linux/macOS 同样可用，命令见文末）
- **Python 3.10 或更高版本**，安装时务必勾选 `Add Python to PATH`
  （下载：https://www.python.org/downloads/ ，检查版本：命令行输入 `python --version`）
- DAIC-WOZ 原始数据集，即一堆 `300_P.zip`、`301_P.zip` 这样的文件，放在同一个文件夹里

## 三步启动

1. 把 U 盘里的 `daic_project_portable` 文件夹（或解压 zip）拷到本机硬盘，
   **不要留在 U 盘上跑**，硬盘读写快得多。
2. 双击 **`setup_and_run.bat`**。
3. 脚本问到数据集路径时，粘贴放着 `*_P.zip` 的文件夹完整路径，回车。

脚本会自动建虚拟环境、装依赖、记住数据路径、自检，最后打开界面
**http://localhost:8501**。

以后再启动，同样双击这个 bat，前面步骤会自动跳过，几秒即开。

## 数据路径记在哪

写在项目根目录的 `local_paths.json`。**这个文件不进 U 盘包**，所以每台电脑
各自记自己的路径，同一份代码在多台机器间直接拷贝不会冲突。

想临时换个数据目录跑一次，不必改文件：

```bash
set DAIC_ZIP_DIR=E:\另一个数据集目录
```

## 数据处理顺序

界面「数据状态」页有对应按钮，也可以用命令行。第一次拿到原始包，按顺序跑：

```bash
.venv\Scripts\python.exe extract_from_zips.py --delete-zip
```

从 zip 里只抽需要的六种文件（转写、COVAREP、FORMANT、CLNF 的 AU/gaze/pose），
**自动跳过体积巨大的 HOG**。`--delete-zip` 抽完即删原包省磁盘；想保留原包就去掉这个参数。

```bash
.venv\Scripts\python.exe build_features.py
```

三路确定性特征 → `outputs/features/all_features.csv`（约 190 列宽表，含各路质量分）。

```bash
.venv\Scripts\python.exe run_experiment.py --lanes tt
```

跑实验。`--lanes` 可组合：`tt` 转写时序、`ac` 声学、`vi` 视觉、`obs` 文本观察、
`d1` 让大模型直接打分（对照组）。例如全模态：`--lanes tt,obs,ac,vi`。

```bash
.venv\Scripts\python.exe make_download_list.py
```

对照官方划分，列出还缺哪些包。

## API key 怎么配

包里的 key **已被抹除**。要跑文本观察 Skill（`obs`/`d1` 两条路）得自己填：
界面 →「Skill 与模型配置」→ 选 provider、填 model 和 api_key → 点「测试连通」
看到绿色 → 「保存全部配置」。存在本机 `data/skill_models.json`，不外传。

用中转站或第三方端点就选 `openai_compatible` 并填 `base_url`。有两个按钮值得先点：
「拉取模型列表」看这个 key 到底能用哪些模型，「检测推理强度实效」验证端点是否
真的支持推理强度（很多中转接受参数但直接丢弃）。

## 两条必须守的纪律

**测试集不许碰。** 47 人测试集只在最终评估用一次。标签加载函数默认不读它，
界面默认隐藏测试样本。**改 Skill 提示词时只看训练集的输出**——否则等于在测试集上调参，
结果作废。

**小样本别下结论。** 所有相关系数都带 bootstrap 95% 置信区间，**区间跨零就是不显著**，
这阶段只能看方向。样本到 60 人以上区间才开始收窄。

## 多人协作注意

改了 `skills/*/skill.md` 提示词，等于换了一个实验版本——提示词内容哈希就是版本号，
会自动记进每条产出。所以**多人不要同时改同一个 skill.md**，会互相覆盖且难追溯。
约定好谁改哪个，或者各自复制成新目录（如 `text_observation_v2`）。

## Linux / macOS

```bash
python3 setup_and_run.py
```

后续命令把 `.venv\Scripts\python.exe` 换成 `.venv/bin/python` 即可。

## 遇到问题

**依赖装不上或很慢**（国内网络）：

```bash
.venv\Scripts\python.exe -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**双击 bat 提示找不到 Python**：装 Python 时没勾 `Add Python to PATH`，
重装并勾选，或手动把 Python 目录加进 PATH。

**界面打不开**：看命令行窗口里的报错；确认 8501 端口没被占用，
被占就改 `--server.port 8502`。

**自检显示"待抽取压缩包 0 个"**：数据集路径填错了，删掉 `local_paths.json`
重新跑 bat，或直接编辑该文件里的 `zip_dir`。
